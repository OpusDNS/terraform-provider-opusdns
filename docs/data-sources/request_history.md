---
page_title: "opusdns_request_history Data Source - opusdns"
subcategory: "Archive"
description: |-
  Lists API request history entries (GET /v1/archive/request-history) for the caller's organization. Returns a single page; use page/page_size to walk results and the filter attributes to narrow them.
---

# opusdns_request_history (Data Source)

Lists API request history entries (`GET /v1/archive/request-history`) for the caller's organization. Returns a single page; use `page`/`page_size` to walk results and the filter attributes to narrow them.

## Example Usage

```terraform
# Latest 50 API requests with status >= 400.
data "opusdns_request_history" "errors" {
  page_size       = 50
  sort_by         = "created_on"
  sort_order      = "desc"
  min_status_code = 400
}

output "request_history_errors" {
  value = [
    for e in data.opusdns_request_history.errors.entries :
    "${e.method} ${e.path} -> ${e.status_code} (${e.duration_ms}ms)"
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `client_ip` (String) Filter by client IP address.
- `max_duration_ms` (Number) Filter to requests whose duration_ms ≤ this value.
- `max_status_code` (Number) Filter to status codes ≤ this value.
- `method` (String) Filter by HTTP method (`GET`, `POST`, etc.).
- `min_duration_ms` (Number) Filter to requests whose duration_ms ≥ this value.
- `min_status_code` (Number) Filter to status codes ≥ this value.
- `page` (Number) 1-indexed page number.
- `page_size` (Number) Number of entries per page.
- `path` (String) Filter by exact request path.
- `performed_by_id` (String) Filter by actor id.
- `performed_by_type` (String) Filter by actor type (`USER`, `API_KEY`, `SYSTEM`).
- `request_started_after` (String) RFC3339 timestamp; only entries started strictly after this are returned.
- `request_started_before` (String) RFC3339 timestamp; only entries started strictly before this are returned.
- `server_request_id` (String) Filter by server-assigned request id.
- `sort_by` (String) Field to sort by (e.g. `created_on`).
- `sort_order` (String) Sort direction: `asc` or `desc`.
- `status_code` (Number) Filter by exact HTTP status code.

### Read-Only

- `entries` (Attributes List) Request history entries matching the filters. (see [below for nested schema](#nestedatt--entries))
- `id` (String) Static identifier for this data source.
- `total_returned` (Number) Number of entries on this page (length of `entries`).

<a id="nestedatt--entries"></a>
### Nested Schema for `entries`

Read-Only:

- `api_key_id` (String) API key used, or empty if a session token was used.
- `created_on` (String) RFC3339 timestamp of when the request was made.
- `duration_ms` (Number) Request duration in milliseconds.
- `ip_address` (String) Client IP, or empty if unset.
- `method` (String) HTTP method.
- `path` (String) Request path.
- `request_id` (String) Unique request identifier.
- `status_code` (Number) HTTP response status code.
- `user_agent` (String) Client user-agent, or empty if unset.
- `user_id` (String) User who made the request, or empty if not user-attributable.
