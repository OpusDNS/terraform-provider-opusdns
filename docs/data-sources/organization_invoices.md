---
page_title: "opusdns_organization_invoices Data Source - opusdns"
subcategory: "Billing"
description: |-
  Lists billing invoices for an organization (GET /v1/organizations/{organization_id}/billing/invoices). Either set organization_id or me = true. Returns a single page; use page/page_size to walk results.
---

# opusdns_organization_invoices (Data Source)

Lists billing invoices for an organization (`GET /v1/organizations/{organization_id}/billing/invoices`). Either set `organization_id` or `me = true`. Returns a single page; use `page`/`page_size` to walk results.

## Example Usage

```terraform
# Most recent invoices for the caller's organization.
data "opusdns_organization_invoices" "recent" {
  me        = true
  page_size = 10
}

output "outstanding_invoices" {
  value = [
    for i in data.opusdns_organization_invoices.recent.invoices :
    "${i.number} (${i.amount} ${i.currency}) due ${i.payment_due_date}"
    if !contains(["succeeded"], i.payment_status)
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `me` (Boolean) When true, resolve the caller's organization id via `/v1/users/me`.
- `organization_id` (String) Organization id. Mutually exclusive with `me`.
- `page` (Number) 1-indexed page number.
- `page_size` (Number) Number of entries per page.

### Read-Only

- `id` (String) Static identifier for this data source.
- `invoices` (Attributes List) Invoices on this page. (see [below for nested schema](#nestedatt--invoices))
- `total_returned` (Number) Number of invoices returned (length of `invoices`).

<a id="nestedatt--invoices"></a>
### Nested Schema for `invoices`

Read-Only:

- `amount` (String) Total invoice amount as a decimal string.
- `currency` (String) Currency code (e.g. `EUR`, `USD`).
- `external_id` (String) External (Lago) invoice identifier.
- `fees_amount` (String) Fees component as a decimal string.
- `file_url` (String) URL to the invoice PDF, or empty if not available.
- `invoice_type` (String) Invoice type (`advance_charges`, `progressive_billing`, ...).
- `issuing_date` (String) ISO-8601 date when the invoice was issued.
- `number` (String) Invoice number.
- `payment_due_date` (String) ISO-8601 date the invoice payment is due.
- `payment_overdue` (Boolean) True when payment is overdue.
- `payment_status` (String) Payment status (`succeeded`, `failed`, ...).
- `status` (String) Invoice status (`draft`, `finalized`, ...).
- `taxes_amount` (String) Taxes component as a decimal string.
