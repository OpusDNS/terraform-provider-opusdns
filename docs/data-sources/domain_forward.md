---
page_title: "opusdns_domain_forward Data Source - opusdns"
subcategory: "Forwarding"
description: |-
  Fetches a single domain forward by its hostname. Use opusdns_domain_forwards to list all domain forwards in a zone.
---

# opusdns_domain_forward (Data Source)

Fetches a single domain forward by its `hostname`. Use `opusdns_domain_forwards` to list all domain forwards in a zone.

## Example Usage

```terraform
# Fetch a single domain forward by its hostname.
data "opusdns_domain_forward" "www" {
  hostname = "www.example.com"
}

output "www_enabled" {
  value = data.opusdns_domain_forward.www.enabled
}

output "www_http_redirects" {
  value = [
    for r in data.opusdns_domain_forward.www.http :
    "${r.request_path} -> ${r.target_protocol}://${r.target_hostname}${r.target_path} (${r.redirect_code})"
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `hostname` (String) The source hostname to fetch the forward for (e.g. `www.example.com`). Semantic equality is used so the trailing-dot form returned by the API is treated as equivalent to the user-supplied form.

### Read-Only

- `created_on` (String) RFC3339 timestamp of when the domain forward was created.
- `enabled` (Boolean) Whether the domain forward is currently active.
- `http` (Attributes List) HTTP protocol redirect rules. (see [below for nested schema](#nestedatt--http))
- `https` (Attributes List) HTTPS protocol redirect rules. (see [below for nested schema](#nestedatt--https))
- `id` (String) Same value as `hostname`.
- `updated_on` (String) RFC3339 timestamp of when the domain forward was last updated.

<a id="nestedatt--http"></a>
### Nested Schema for `http`

Read-Only:

- `redirect_code` (Number) The HTTP redirect status code.
- `request_path` (String) The source path to match.
- `target_hostname` (String) The destination hostname.
- `target_path` (String) The destination path.
- `target_protocol` (String) The destination protocol (`http` or `https`).


<a id="nestedatt--https"></a>
### Nested Schema for `https`

Read-Only:

- `redirect_code` (Number) The HTTP redirect status code.
- `request_path` (String) The source path to match.
- `target_hostname` (String) The destination hostname.
- `target_path` (String) The destination path.
- `target_protocol` (String) The destination protocol (`http` or `https`).
