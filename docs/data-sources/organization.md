---
page_title: "opusdns_organization Data Source - opusdns"
subcategory: "Team"
description: |-
  Fetches a single organization from /v1/organizations/{id}. Set me = true to look up the authenticated caller's own organization (resolved via /v1/users/me).
---

# opusdns_organization (Data Source)

Fetches a single organization from `/v1/organizations/{id}`. Set `me = true` to look up the authenticated caller's own organization (resolved via `/v1/users/me`).

## Example Usage

```terraform
# Look up the authenticated caller's organization. The provider resolves the
# id by calling GET /v1/users/me and then GET /v1/organizations/{id}.
data "opusdns_organization" "current" {
  me = true
}

output "current_org_name" {
  value = data.opusdns_organization.current.name
}

# Or look up a specific organization by id.
# data "opusdns_organization" "specific" {
#   organization_id = "organization_01jnh0v027fz2r0pcbavf9qtyy"
# }
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `me` (Boolean) When true, resolve the authenticated user's organization id via `/v1/users/me` instead of using `organization_id`.
- `organization_id` (String) Organization id to look up. Required unless `me = true`.

### Read-Only

- `address_1` (String)
- `address_2` (String)
- `business_number` (String)
- `city` (String)
- `country_code` (String)
- `created_on` (String)
- `currency` (String)
- `default_locale` (String)
- `id` (String) Mirror of `organization_id`.
- `name` (String) Organization name.
- `parent_organization_id` (String)
- `postal_code` (String)
- `state` (String)
- `status` (String)
- `tax_id` (String)
- `tax_id_type` (String)
- `tax_rate` (String)
