---
page_title: "opusdns_user Data Source - opusdns"
subcategory: "Team"
description: |-
  Fetches a single user. Set me = true to look up the authenticated caller via GET /v1/users/me, otherwise provide user_id to look up GET /v1/users/{user_id}.
---

# opusdns_user (Data Source)

Fetches a single user. Set `me = true` to look up the authenticated caller via `GET /v1/users/me`, otherwise provide `user_id` to look up `GET /v1/users/{user_id}`.

## Example Usage

```terraform
# Look up the authenticated caller's user record via GET /v1/users/me.
data "opusdns_user" "current" {
  me = true
}

output "current_user_email" {
  value = data.opusdns_user.current.email
}

# Or look up a specific user by id.
# data "opusdns_user" "specific" {
#   user_id = "user_01jnh0yy31eqmbxyrpaa70ph70"
# }
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `me` (Boolean) When true, resolve via `/v1/users/me`.
- `user_id` (String) User id to look up. Required unless `me = true`.

### Read-Only

- `created_on` (String)
- `email` (String)
- `first_name` (String)
- `id` (String) Mirror of `user_id`.
- `last_name` (String)
- `locale` (String)
- `organization_id` (String)
- `phone` (String)
- `status` (String)
- `updated_on` (String)
- `username` (String)
