---
page_title: "opusdns_contact Resource - opusdns"
subcategory: "Contacts"
description: |-
  Manages a contact in OpusDNS. Contacts are used for domain registration.
---

# opusdns_contact (Resource)

Manages a contact in OpusDNS. Contacts are used for domain registration.

## Example Usage

```terraform
resource "opusdns_contact" "admin" {
  first_name  = "Jane"
  last_name   = "Doe"
  email       = "jane.doe@example.com"
  phone       = "+12125550199"
  street      = "123 Main St"
  city        = "New York"
  state       = "NY"
  postal_code = "10001"
  country     = "US"
  disclose    = false
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `city` (String) The city.
- `country` (String) The two-letter country code (ISO 3166-1 alpha-2, e.g., `US`).
- `email` (String) The contact's email address.
- `first_name` (String) The contact's first name.
- `last_name` (String) The contact's last name.
- `phone` (String) The contact's phone number in E.164 format (e.g., `+12125551234`). The server normalizes punctuation, so semantic equality is used when formatting differs but the digits are the same.
- `postal_code` (String) The postal or ZIP code.
- `street` (String) The street address.

### Optional

- `disclose` (Boolean) Whether to publicly disclose contact information (WHOIS). Defaults to `false`.
- `fax` (String) The contact's fax number in E.164 format. The server normalizes punctuation, so semantic equality is used when formatting differs but the digits are the same.
- `org` (String) The contact's organization name.
- `state` (String) The state or province.
- `title` (String) The contact's title (e.g., `Mr.`, `Dr.`).

### Read-Only

- `contact_id` (String) The unique identifier for the contact.
- `id` (String) The contact ID.

## Import

Import is supported using the following syntax:

```shell
terraform import opusdns_contact.example <contact_id>
```
